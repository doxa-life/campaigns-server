import type { FieldDefinition } from '../types'

export const field: FieldDefinition = {
  key: 'believers_count',
  labelKey: 'peopleGroups.fields.believers_count',
  type: 'number',
  category: 'engagement'
}
