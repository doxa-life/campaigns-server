import type { FieldDefinition } from '../types'

export const field: FieldDefinition = {
  key: 'workers_long_term',
  labelKey: 'peopleGroups.fields.workers_long_term',
  type: 'boolean',
  category: 'workers'
}
